import SwiftUI

`class User {
    var email: String
    var password: String
    var favorites: [Show]

    init(email: String, password: String) {
        self.email = email
        self.password = password
        self.favorites = []
    }
}`